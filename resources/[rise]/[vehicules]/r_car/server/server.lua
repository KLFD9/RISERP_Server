-- Écoute de l'événement personnalisé 'server:UpdateFuelLevel'
RegisterNetEvent('server:UpdateFuelLevel')
AddEventHandler('server:UpdateFuelLevel', function(playerCoords, fuelLevel)
    local _source = source
    
end)